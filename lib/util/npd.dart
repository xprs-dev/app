// Re-exported from the shared `reticulum` package (single source of truth).
// The NOSTR Probe Datagram: a connectionless, NOSTR-encrypted "do you have
// this?" that replaces a Reticulum link handshake — see the package for why.
export 'package:reticulum/src/util/npd.dart';
